js.extend('lsn.ext.browse.qview', function (js) {

  var proto = {};
  this.Text = function () {
  };
  this.Text.prototype = proto;
  
  proto.load = function (tnode) {
  };

  proto.unload = function () {
  };

});
