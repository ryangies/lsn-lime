# HashFile 2.1

background-image => %{
  label => Background image
  description => Select an image which will be used as the background for the selected area.
  example => /images/photo.jpg
  prefix => url(
  suffix => )
}
